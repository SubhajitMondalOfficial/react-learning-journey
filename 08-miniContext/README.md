# 1st Step

### Creating a folder named with "context" then add a file "UserContext.js" And then simpelly write this code 

```js
import React from 'React'

const UserContext = React.createContext()

export default UserContext;
 
```


# 2st Step

### Creating the next file named with "UserCOntextProvider.jsx" 
